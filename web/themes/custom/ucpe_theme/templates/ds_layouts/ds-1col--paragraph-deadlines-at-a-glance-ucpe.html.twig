{#
/**
 * @file
 * Display Suite 1 column template.
 *
 * Available variables:
 * - ds_content_wrapper: wrapper around content
 * - attributes: content region attributes
 * - ds_content: content region
 */
#}
<div class="deadline">
  <span class="round">{{ ds_content.field_round }}</span>
  <span class="date">{{ ds_content.field_deadline }}</span>
</div>
